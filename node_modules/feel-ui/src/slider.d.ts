import { EventEmitter } from "angular2/core";
export declare class Slider {
    value: number;
    valueChange: EventEmitter<{}>;
    private _offset;
    private _getOffset();
    private _buttonElement;
    maximum: number;
    constructor();
    private _calculateOffset();
    private _startDrag(event);
    private _handleDrag(event);
    private _stopDrag();
}
